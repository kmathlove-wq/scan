# P04-스캔(Scan)

브라우저에서 종이·문서를 카메라로 찍어 자동으로 네 모서리를 인식하고 반듯하게 편 뒤, 필터를 골라 이미지(PNG/JPG) 또는 PDF로 저장하는 한 페이지짜리 웹 앱. 서버 없이 전부 사용자 기기 안에서 처리한다. 깃허브 페이지로 배포한다(저장소 `scan`).
<!-- 상위 CLAUDE.md 규칙을 여기 재서술하지 말 것 -->
